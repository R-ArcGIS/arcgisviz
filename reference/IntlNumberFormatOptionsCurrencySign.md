# IntlNumberFormatOptionsCurrencySign

One of `"accounting"`, `"standard"`, `NA`.

## Usage

``` r
IntlNumberFormatOptionsCurrencySign(value = NA_character_)
```

## Arguments

- value:

  String. One of `"accounting"`, `"standard"`, `NA`.

## Value

An object of class `IntlNumberFormatOptionsCurrencySign`.

## Additional properties

- `@variants`:

  Character vector. The values this enum allows.

- `@allow_na`:

  Bool. Whether `NA_character_` is allowed.
