# WebChartCalendarDatePartsUnits

One of `"dayOfMonth"`, `"dayOfWeek"`, `"dayOfYear"`, `"hourOfDay"`,
`"monthOfYear"`, `"minuteOfDay"`, `"weekOfYear"`, `"quarterOfYear"`,
`NA`.

## Usage

``` r
WebChartCalendarDatePartsUnits(value = NA_character_)
```

## Arguments

- value:

  String. One of `"dayOfMonth"`, `"dayOfWeek"`, `"dayOfYear"`,
  `"hourOfDay"`, `"monthOfYear"`, `"minuteOfDay"`, `"weekOfYear"`,
  `"quarterOfYear"`, `NA`.

## Value

An object of class `WebChartCalendarDatePartsUnits`.

## Additional properties

- `@variants`:

  Character vector. The values this enum allows.

- `@allow_na`:

  Bool. Whether `NA_character_` is allowed.
