# ModelTypes

The `@arcgis/charts-components` chart-type strings accepted by
`createModel()`, e.g. `"barChart"`, `"lineChart"`, `"scatterplot"`.

## Usage

``` r
ModelTypes(value = resolved_default)
```
