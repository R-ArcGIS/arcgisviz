# IStatisticDefinitionStatisticParametersOrderBy

One of `"asc"`, `"desc"`, `NA`.

## Usage

``` r
IStatisticDefinitionStatisticParametersOrderBy(value = NA_character_)
```

## Arguments

- value:

  String. One of `"asc"`, `"desc"`, `NA`.

## Value

An object of class `IStatisticDefinitionStatisticParametersOrderBy`.

## Additional properties

- `@variants`:

  Character vector. The values this enum allows.

- `@allow_na`:

  Bool. Whether `NA_character_` is allowed.
