# ArcChart

ArcChart

## Usage

``` r
ArcChart(
  data = NULL,
  chart_type = NA_character_,
  x = NA_character_,
  y = NA_character_,
  stat = NA_character_,
  labs = list()
)
```

## Properties

`@webchart` is computed from the mapping, not stored - assigning to it
has no effect.
