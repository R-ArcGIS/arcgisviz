# Line chart

Line chart

## Usage

``` r
arc_line(.data, x, y)
```

## Arguments

- .data:

  A data frame (or similar) the chart's fields will come from.

- x, y:

  Bare column names from `.data` (tidy eval).
