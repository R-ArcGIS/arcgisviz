% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-chart.R
\name{set_stat}
\alias{set_stat}
\title{Set a chart's statistical transformation}
\usage{
set_stat(chart, stat)
}
\arguments{
\item{chart}{An \code{ArcChart}, from \code{\link[=arc_chart]{arc_chart()}} with \code{\link[=set_type]{set_type()}} already
called.}

\item{stat}{One of \code{"identity"}, \code{"count"}, \code{"sum"}, \code{"mean"}, \code{"min"},
\code{"max"}, \code{"sd"}, \code{"var"}.}
}
\value{
\code{chart}, with its stat set.
}
\description{
How \code{y} is derived from the data, in the sense ggplot2's \code{stat} argument
means it. \code{"identity"} plots \code{y} verbatim, one mark per row (\code{geom_col()});
every other value aggregates \code{y} grouped by \code{x} (\code{geom_bar()}, or
\code{stat_summary()}). \code{"count"} needs no \code{y} - it counts rows per \code{x}.
}
\details{
Bar and line charts only; scatterplots ignore it.
}
