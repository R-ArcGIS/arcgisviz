% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-proxy.R
\name{arc_selected}
\alias{arc_selected}
\title{Read a map selection}
\usage{
arc_selected(x, layer = NULL)
}
\arguments{
\item{x}{Defines which selection event to read.}

\item{layer}{default \code{NULL}. Defines which layer's ids to return, by name.
\code{NULL} returns every selected id, across layers.}
}
\value{
An integer vector of object ids.
}
\description{
Pulls the object ids out of an \verb{input$<output_id>_selection} event. A
selection can span several layers, so \code{layer} narrows it to one.
}
\examples{
event <- list(
  count = 3,
  layers = list(list(layer = "Counties", objectIds = c(1, 2, 5)))
)

arc_selected(event)
arc_selected(event, layer = "Counties")
}
