% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-chart.R
\name{set_legend}
\alias{set_legend}
\title{Position or hide a chart's legend}
\usage{
set_legend(chart, visible = NULL, position = NULL, title = NULL)
}
\arguments{
\item{chart}{Defines which chart to modify.}

\item{visible}{default \code{NULL}. Defines whether the legend is drawn.}

\item{position}{default \code{NULL}. Defines where it sits, one of \code{"right"},
\code{"left"}, \code{"top"}, or \code{"bottom"}.}

\item{title}{default \code{NULL}. Defines the text above the legend.}
}
\value{
\code{chart}, with its legend set.
}
\description{
Moves, titles, or removes the key naming a chart's groups. A legend needs
something to name, so a chart only has one once \code{\link[=set_color]{set_color()}} has grouped
it, and it is drawn by default from then on. A heat chart is the
exception: its legend is the colour gradient, so it always has one.
}
\details{
Asking for a legend on a chart that cannot show one is an error rather
than a silent no-op - \code{visible = TRUE} cannot conjure a key out of a
single series.
}
\examples{
df <- data.frame(
  species = c("a", "a", "b"),
  island = c("x", "y", "x")
)

arc_bar(df, species) |>
  set_color(island) |>
  set_legend(position = "bottom", title = "Island")
}
