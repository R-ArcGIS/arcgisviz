% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-chart.R
\name{arc_pie}
\alias{arc_pie}
\alias{set_pie}
\title{Pie chart}
\usage{
arc_pie(.data, x, y = NULL, hole = NULL, labels = NULL, inside = NULL)

set_pie(chart, ..., hole = NULL, labels = NULL, inside = NULL)
}
\arguments{
\item{.data}{Defines which data frame the chart draws its fields from.}

\item{x}{Defines which column the slices are cut from.}

\item{y}{default \code{NULL}. Defines which column sizes each slice. Omit it to
count rows instead.}

\item{hole}{default \code{NULL}. Defines the size of the hole in the middle as
a percentage of the radius, turning the pie into a doughnut.}

\item{labels}{default \code{NULL}. Defines what each slice's label shows, any
of \code{"category"}, \code{"value"}, and \code{"percent"}.}

\item{inside}{default \code{NULL}. Defines whether the labels sit inside the
slices.}

\item{chart}{Defines which chart to modify.}

\item{...}{These dots are for future extensions and must be empty.}
}
\value{
An \code{ArcChart}.
}
\description{
Draws one slice per value of \code{x}, sized by how many rows fall into it. Use
\code{\link[=set_stat]{set_stat()}} for any other aggregation, or \code{y} for values you have already
summarised.
}
\details{
\code{set_pie()} reaches the same options later, for charts built with
\code{\link[=set_type]{set_type()}} rather than this shortcut.
}
\examples{
df <- data.frame(species = c("a", "a", "b"), mass = c(1, 5, 3))

arc_pie(df, species, hole = 60)
}
