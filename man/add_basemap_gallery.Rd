% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-shortcuts.R
\name{add_basemap_gallery}
\alias{add_basemap_gallery}
\alias{add_basemap_toggle}
\title{Add a basemap gallery}
\usage{
add_basemap_gallery(map, position = NULL, expand = FALSE, disabled = NULL, ...)

add_basemap_toggle(
  map,
  position = NULL,
  expand = FALSE,
  next_basemap = NULL,
  show_title = NULL,
  ...
)
}
\arguments{
\item{map}{Defines which map or \code{\link[=arc_map_proxy]{arc_map_proxy()}} to modify.}

\item{position}{default \code{NULL}. Defines which corner it sits in, one of
\code{"top-left"}, \code{"top-right"}, \code{"bottom-left"}, \code{"bottom-right"}, or the
direction-aware \code{"top-start"}, \code{"top-end"}, \code{"bottom-start"},
\code{"bottom-end"}. Each widget has its own default.}

\item{expand}{default \code{FALSE}. Defines whether the widget is collapsed
behind a button rather than drawn open. Worth it for the panels - the
legend, layer list, basemap gallery - which otherwise cover the map.
Collapsed widgets sharing a corner close each other when opened.}

\item{disabled}{default \code{NULL}. Defines whether the gallery is greyed out.}

\item{...}{Named properties of the component itself, written either
\code{snake_case} or \code{camelCase}. \code{\link[=map_widgets]{map_widgets()}} lists what each one takes.}

\item{next_basemap}{default \code{NULL}. Defines the basemap to toggle to, as an
id from \link{Basemaps}.}

\item{show_title}{default \code{NULL}. Defines whether the basemap's name is
drawn under the button.}
}
\value{
\code{map}, with the widget added.
}
\description{
Lets the reader swap the basemap for any in the gallery. \verb{add_basemap_ toggle()} is the two-basemap version, a single button.
}
\examples{
arc_map() |> add_basemap_gallery(expand = TRUE)
arc_map() |> add_basemap_toggle(next_basemap = "satellite")
}
