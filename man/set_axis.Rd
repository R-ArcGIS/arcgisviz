% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-chart.R
\name{set_axis}
\alias{set_axis}
\title{Set an axis}
\usage{
set_axis(
  chart,
  axis,
  ...,
  limits = NULL,
  log = NULL,
  zero_line = NULL,
  integer_only = NULL,
  tick_spacing = NULL,
  buffer = NULL,
  visible = NULL
)
}
\arguments{
\item{chart}{Defines which chart to modify.}

\item{axis}{Defines which axis to change, either \code{"x"} or \code{"y"}.}

\item{...}{These dots are for future extensions and must be empty.}

\item{limits}{default \code{NULL}. Defines the two values the axis spans, where
\code{NA} leaves that bound to the chart.}

\item{log}{default \code{NULL}. Defines whether the axis is logarithmic.}

\item{zero_line}{default \code{NULL}. Defines whether a line is drawn at zero.}

\item{integer_only}{default \code{NULL}. Defines whether only whole numbers are
labelled.}

\item{tick_spacing}{default \code{NULL}. Defines the smallest gap between ticks,
which the chart may still widen to fit.}

\item{buffer}{default \code{NULL}. Defines whether space is added around the
series.}

\item{visible}{default \code{NULL}. Defines whether the axis is drawn at all.}
}
\value{
\code{chart}, with that axis set.
}
\description{
Overrides how one axis is scaled and drawn. Omitted arguments leave that
part of the axis as the chart would draw it.
}
\examples{
df <- data.frame(species = c("a", "b", "c"), mass = c(1, 5, 3))

arc_col(df, species, mass) |>
  set_axis("y", limits = c(0, 10), zero_line = TRUE)
}
