% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-shortcuts.R
\name{add_zoom}
\alias{add_zoom}
\alias{add_home}
\alias{add_compass}
\alias{add_fullscreen}
\alias{add_locate}
\alias{add_track}
\title{Add a navigation control}
\usage{
add_zoom(map, position = NULL, layout = NULL, visual_scale = NULL, ...)

add_home(map, position = NULL, visual_scale = NULL, ...)

add_compass(map, position = NULL, visual_scale = NULL, ...)

add_fullscreen(map, position = NULL, visual_scale = NULL, ...)

add_locate(map, position = NULL, scale = NULL, visual_scale = NULL, ...)

add_track(map, position = NULL, scale = NULL, visual_scale = NULL, ...)
}
\arguments{
\item{map}{Defines which map or \code{\link[=arc_map_proxy]{arc_map_proxy()}} to modify.}

\item{position}{default \code{NULL}. Defines which corner it sits in, one of
\code{"top-left"}, \code{"top-right"}, \code{"bottom-left"}, \code{"bottom-right"}, or the
direction-aware \code{"top-start"}, \code{"top-end"}, \code{"bottom-start"},
\code{"bottom-end"}. Each widget has its own default.}

\item{layout}{default \code{NULL}. Defines whether the two zoom buttons stack,
\code{"vertical"} or \code{"horizontal"}.}

\item{visual_scale}{default \code{NULL}. Defines the button's size relative to
its default, as a multiplier.}

\item{...}{Named properties of the component itself, written either
\code{snake_case} or \code{camelCase}. \code{\link[=map_widgets]{map_widgets()}} lists what each one takes.}

\item{scale}{default \code{NULL}. Defines the scale the map zooms to when the
reader is found.}
}
\value{
\code{map}, with the widget added.
}
\description{
The map's buttons: zoom in and out, return to the starting view, reorient
north, fill the screen, and find the reader. They take no arguments beyond
where they sit.
}
\details{
\code{add_locate()} moves the map to the reader's position once;
\code{add_track()} follows it as it changes. Both ask the browser for
permission, and both need a secure context - \verb{https://} or \code{localhost}.
}
\examples{
arc_map() |>
  add_zoom(layout = "horizontal") |>
  add_home() |>
  add_compass()
}
