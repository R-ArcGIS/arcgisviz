% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-chart.R
\name{set_size}
\alias{set_size}
\title{Map a column to marker size}
\usage{
set_size(chart, size, range = NULL, scale = "linear")
}
\arguments{
\item{chart}{Defines which chart to modify.}

\item{size}{Defines which numeric column the marker sizes are drawn from.}

\item{range}{default \code{NULL}. Defines the smallest and largest marker size
as a length-2 numeric vector. \code{NULL} keeps the SDK's own 5 to 30.}

\item{scale}{default \code{"linear"}. Defines how values map onto that range,
either \code{"linear"} or \code{"log"}.}
}
\value{
\code{chart}, with its marker sizes set.
}
\description{
Scales each marker by the value of a numeric column, turning a
scatterplot into a bubble chart. Only scatterplots draw markers, so this
does not apply to the other chart types.
}
\examples{
df <- data.frame(len = c(1, 5, 3), dep = c(2, 4, 6), mass = c(10, 40, 25))

arc_scatter(df, len, dep) |>
  set_size(mass, range = c(4, 20))
}
