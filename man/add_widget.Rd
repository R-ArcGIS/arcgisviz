% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-widgets.R
\name{add_widget}
\alias{add_widget}
\alias{remove_widget}
\title{Add a widget to a map}
\usage{
add_widget(map, widget, position = NULL, ...)

remove_widget(map, widget = NULL)
}
\arguments{
\item{map}{Defines which map or \code{\link[=arc_map_proxy]{arc_map_proxy()}} to modify.}

\item{widget}{default \code{NULL}. Defines which widget to take off, by the name
it was added with. \code{NULL} removes every widget.}

\item{position}{default \code{NULL}. Defines which corner it sits in, one of
\code{"top-left"}, \code{"top-right"}, \code{"bottom-left"}, \code{"bottom-right"}, or the
direction-aware \code{"top-start"}, \code{"top-end"}, \code{"bottom-start"},
\code{"bottom-end"}. Each widget has its own default.}

\item{...}{Named properties of the component itself, written either
\code{snake_case} or \code{camelCase}. \code{\link[=map_widgets]{map_widgets()}} lists what each one takes.}
}
\value{
\code{map}, with the widget added.
}
\description{
Puts one of the Maps SDK's own components on the map - a legend, a layer
list, a search box, a basemap gallery. Each is a real web component that
finds the map itself, so nothing has to be wired up.
}
\details{
Adding a widget that is already on the map replaces it, which is what makes
\code{add_widget()} idempotent across \code{\link[=arc_update]{arc_update()}} flushes. \code{\link[=remove_widget]{remove_widget()}}
takes one off again.
}
\examples{
arc_map() |>
  add_widget("legend") |>
  add_widget("layer-list", position = "top-right", show_filter = TRUE)
}
