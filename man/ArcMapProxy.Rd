% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-proxy.R
\name{ArcMapProxy}
\alias{ArcMapProxy}
\alias{arc_map_proxy}
\title{Update a rendered map from the Shiny server}
\usage{
ArcMapProxy(
  layers = list(),
  basemap = NA_character_,
  center = integer(0),
  zoom = NA_real_,
  extent = list(),
  output_id = NA_character_,
  session = NULL
)

arc_map_proxy(output_id, session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{output_id}{Defines which \code{\link[=arcgisMapOutput]{arcgisMapOutput()}} to update.}

\item{session}{default \code{\link[shiny:getDefaultReactiveDomain]{shiny::getDefaultReactiveDomain()}}. Defines the
Shiny session to send through.}
}
\value{
An \code{ArcMapProxy}, which \code{\link[=add_layer]{add_layer()}} and the map \verb{set_*()}
functions accept.
}
\description{
\code{arc_map_proxy()} wraps a map that is already on screen. \code{\link[=set_basemap]{set_basemap()}},
\code{\link[=set_view]{set_view()}} and \code{\link[=add_layer]{add_layer()}} all work on the result, and \code{\link[=arc_update]{arc_update()}}
sends the accumulated changes to the browser as one message.
}
\details{
Only what changed crosses the wire. A basemap or view change carries no
data at all, and a layer already drawn is never resent - it is filtered,
highlighted, hidden or removed in place by \code{\link[=set_filter]{set_filter()}},
\code{\link[=set_selection]{set_selection()}}, \code{\link[=set_layer]{set_layer()}} and \code{\link[=remove_layer]{remove_layer()}}.

A layer added through a proxy must be named, because the name is what
identifies it to those functions. Adding one whose name is already on the
map replaces it.
}
\examples{
# Inside a Shiny server:
if (interactive()) {
  arc_map_proxy("map") |>
    set_basemap("satellite") |>
    arc_update()
}
}
