% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-chart.R
\name{arc_gauge}
\alias{arc_gauge}
\alias{set_gauge}
\title{Gauge}
\usage{
arc_gauge(
  .data,
  x,
  stat = "mean",
  feature = NULL,
  hole = NULL,
  angles = NULL,
  needle = NULL
)

set_gauge(
  chart,
  ...,
  feature = NULL,
  hole = NULL,
  angles = NULL,
  needle = NULL
)
}
\arguments{
\item{.data}{Defines which data frame the chart draws its fields from.}

\item{x}{Defines which numeric column the reading comes from.}

\item{stat}{default \code{"mean"}. Defines how the column is reduced to one
value. See \code{\link[=set_stat]{set_stat()}}.}

\item{feature}{default \code{NULL}. Defines which row to read verbatim, by
position. Setting it overrides \code{stat}.}

\item{hole}{default \code{NULL}. Defines the size of the hole in the middle as
a percentage of the radius.}

\item{angles}{default \code{NULL}. Defines the dial's start and end angle in
degrees, as \code{c(start, end)}.}

\item{needle}{default \code{NULL}. Defines whether the needle is drawn.}

\item{chart}{Defines which chart to modify.}

\item{...}{These dots are for future extensions and must be empty.}
}
\value{
An \code{ArcChart}.
}
\description{
Draws a single number on a dial. A gauge reads one value, so \code{x} names the
column it comes from and \code{\link[=set_stat]{set_stat()}} decides how the column is reduced to
that value - or \code{feature} picks one row verbatim.
}
\details{
\code{set_gauge()} reaches the same options later, for charts built with
\code{\link[=set_type]{set_type()}} rather than this shortcut.
}
\examples{
df <- data.frame(mass = c(1, 5, 3))

arc_gauge(df, mass, stat = "mean")
}
