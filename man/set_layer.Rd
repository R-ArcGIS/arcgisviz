% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-proxy.R
\name{set_layer}
\alias{set_layer}
\alias{remove_layer}
\title{Show, hide, or fade a layer that is already drawn}
\usage{
set_layer(proxy, layer, visible = NULL, opacity = NULL)

remove_layer(proxy, layer = NULL)
}
\arguments{
\item{proxy}{Defines which \code{\link[=arc_map_proxy]{arc_map_proxy()}} to modify.}

\item{layer}{Defines which layer, by the \code{name} it was added with.
\code{remove_layer()} accepts \code{NULL} to remove every layer.}

\item{visible}{default \code{NULL}. Defines whether the layer is drawn.}

\item{opacity}{default \code{NULL}. Defines the layer opacity, from \code{0} to \code{1}.}
}
\value{
\code{proxy}, invisibly.
}
\description{
Changes one layer's appearance without resending it. \code{remove_layer()}
takes it off the map instead.
}
\examples{
if (interactive()) {
  arc_map_proxy("map") |>
    set_layer("Counties", visible = FALSE)
}
}
