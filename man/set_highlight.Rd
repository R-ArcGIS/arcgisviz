% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map.R
\name{set_highlight}
\alias{set_highlight}
\title{Style the selection highlight}
\usage{
set_highlight(
  map,
  color = NULL,
  halo_opacity = NULL,
  fill_opacity = NULL,
  shadow_color = NULL,
  shadow_opacity = NULL,
  shadow_difference = NULL
)
}
\arguments{
\item{map}{Defines which map or proxy to modify.}

\item{color}{default \code{NULL}. Defines the highlight colour, as a name or hex
string.}

\item{halo_opacity}{default \code{NULL}. Defines the opacity of the outline drawn
around a selected feature, from \code{0} to \code{1}.}

\item{fill_opacity}{default \code{NULL}. Defines the opacity of the fill drawn
over a selected feature, from \code{0} to \code{1}.}

\item{shadow_color}{default \code{NULL}. Defines the colour of the shadow cast
over everything that is \emph{not} selected.}

\item{shadow_opacity}{default \code{NULL}. Defines that shadow's opacity, from
\code{0} to \code{1}.}

\item{shadow_difference}{default \code{NULL}. Defines how much the shadow dims
the unselected features, from \code{0} to \code{1}.}
}
\value{
\code{map}, with the highlight style set.
}
\description{
Changes how selected features are drawn. Works on an \code{\link[=arc_map]{arc_map()}} before it
is rendered and on an \code{\link[=arc_map_proxy]{arc_map_proxy()}} after, and applies to every
selection - the ones \code{\link[=set_selection]{set_selection()}} makes, the ones a click on a
\code{selectable} layer makes, and the ones \code{\link[=arc_select]{arc_select()}} draws.
}
\examples{
arc_map() |>
  set_highlight(color = "orange", fill_opacity = 0.4)
}
