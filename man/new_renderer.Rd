% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/types-renderer.R
\name{new_renderer}
\alias{new_renderer}
\title{Create a renderer}
\usage{
new_renderer(type = "simple", ...)
}
\arguments{
\item{type}{default \code{"simple"}. Defines which renderer to build, either
\code{"simple"} or \code{"unique-value"}.}

\item{...}{Defines the renderer's properties, all named. See
\link{ISimpleRenderer} and \link{IUniqueValueRenderer} for what each kind takes.}
}
\value{
An \link{ISimpleRenderer} or an \link{IUniqueValueRenderer}.
}
\description{
Builds a renderer to hand to \code{\link[=add_renderer]{add_renderer()}}. \code{type} picks which kind and
\code{...} sets that kind's properties, so this is one function instead of
remembering which class goes with which symbology.
}
\details{
A \code{"simple"} renderer draws every feature the same way, optionally varying
colour by a \code{colorInfo} visual variable. A \code{"unique-value"} renderer draws
one symbol per value of a field.
}
\examples{
new_renderer("simple", symbol = ISimpleMarkerSymbol(size = 8))

new_renderer("unique-value", field1 = "species")
}
