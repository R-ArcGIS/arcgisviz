% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{esri_palettes}
\alias{esri_palettes}
\title{Browse the Esri colour ramps}
\usage{
esri_palettes(type = NULL, color_mode = NULL, hue = NULL, tag = NULL)
}
\arguments{
\item{type}{default \code{NULL}. Keeps only ramps of these types, any of
\code{"sequential"}, \code{"diverging"}, or \code{"categorical"}.}

\item{color_mode}{default \code{NULL}. Keeps only ramps drawn for a \code{"light"}
or \code{"dark"} background.}

\item{hue}{default \code{NULL}. Keeps ramps drawing on any of these colour
families, such as \code{"blues"} or \code{"reds"}.}

\item{tag}{default \code{NULL}. Keeps ramps carrying all of these tags, such as
\code{"heatmap"} or \code{"colorblind-friendly"}.}
}
\value{
A data frame of ramps, one row each.
}
\description{
Lists every ramp \code{\link[=set_color]{set_color()}} accepts by name, with the tags the ArcGIS
SDK ships alongside them. Call it with no arguments for all of them, or
narrow it with any combination of the arguments below.
}
\details{
The result has one row per ramp and these columns.

\describe{
\item{palette}{The name to pass to \code{\link[=set_color]{set_color()}}.}
\item{type}{\code{"sequential"}, \code{"diverging"}, \code{"categorical"}, or \code{NA}.}
\item{color_mode}{\code{"light"} or \code{"dark"}, the background the ramp is
drawn for.}
\item{colorblind_friendly}{Whether Esri tags the ramp as such.}
\item{n_stops}{How many colours the ramp defines.}
\item{hues}{The colour families the ramp draws on.}
\item{tags}{Every tag, including the ones above.}
}
}
\examples{
esri_palettes(type = "diverging", color_mode = "dark")
}
