% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-map-proxy.R
\name{arc_draw_selection}
\alias{arc_draw_selection}
\title{Select features by drawing on the map}
\usage{
arc_draw_selection(proxy, tool = "rectangle", mode = "replace", layer = NULL)
}
\arguments{
\item{proxy}{Defines which \code{\link[=arc_map_proxy]{arc_map_proxy()}} to draw on.}

\item{tool}{default \code{"rectangle"}. Defines what the reader draws, one of
\code{"rectangle"}, \code{"polygon"}, \code{"lasso"}, \code{"circle"}, or \code{"point"}.}

\item{mode}{default \code{"replace"}. Defines what the drawn shape does to the
existing selection, one of \code{"replace"}, \code{"add"}, or \code{"remove"}.}

\item{layer}{default \code{NULL}. Defines which layers can be selected in, by
name. \code{NULL} means every layer on the map.}
}
\value{
\code{proxy}, invisibly.
}
\description{
Hands the reader a drawing tool. Whatever the shape they draw covers becomes
the selection, which arrives back as \verb{input$<output_id>_selection} the same
way \code{\link[=set_selection]{set_selection()}}'s does. The tool is live from the moment this is
called and is put away once the shape is finished.
}
\examples{
if (interactive()) {
  arc_map_proxy("map") |>
    arc_draw_selection(tool = "lasso")
}
}
