% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{new_legend_options}
\alias{new_legend_options}
\title{Describe a renderer in the legend}
\usage{
new_legend_options(title = NULL, visible = NULL, order = NULL)
}
\arguments{
\item{title}{default \code{NULL}. Defines the text naming this renderer or ramp.}

\item{visible}{default \code{NULL}. Defines whether it appears in the legend at
all. Has no effect under a \code{"unique-value"} renderer, which the spec does
not allow it on.}

\item{order}{default \code{NULL}. Defines which end comes first, either
\code{"ascending"} or \code{"descending"}.}
}
\value{
An \link{ILegendOptions}.
}
\description{
Names and orders what a renderer or a colour ramp contributes to the map's
legend. Pass the result as the \code{legendOptions} of \code{\link[=new_renderer]{new_renderer()}} or of a
colour visual variable.
}
\details{
A continuous ramp is otherwise unlabelled, so \code{title} is the usual reason
to reach for this.
}
\examples{
new_legend_options(title = "Births, 1974", order = "descending")
}
