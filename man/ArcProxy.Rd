% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-proxy.R
\name{ArcProxy}
\alias{ArcProxy}
\alias{arc_proxy}
\title{Update a rendered chart from the Shiny server}
\usage{
ArcProxy(
  data = NULL,
  chart_type = NA_character_,
  x = NA_character_,
  y = NA_character_,
  stat = NA_character_,
  labs = list(),
  color = list(),
  size = list(),
  tooltip = character(0),
  axes = list(),
  flipped = NA,
  position = NA_character_,
  series_opts = list(),
  config_opts = list(),
  output_id = NA_character_,
  session = NULL
)

arc_proxy(output_id, chart, session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{output_id}{Defines which \code{\link[=arcgisChartOutput]{arcgisChartOutput()}} to update.}

\item{session}{default \code{\link[shiny:getDefaultReactiveDomain]{shiny::getDefaultReactiveDomain()}}. Defines the
Shiny session to send through.}

\item{chart}{Defines the chart currently rendered there, so that the
\verb{set_*()} functions can validate against the same data.}
}
\value{
An \code{ArcProxy}, which every \verb{set_*()} function accepts.
}
\description{
\code{arc_proxy()} wraps a chart that is already on screen. Every \verb{set_*()}
function works on the result, and \code{\link[=arc_update]{arc_update()}} sends the accumulated
changes to the browser.
}
\details{
The data never crosses the wire again. Only the configuration is resent,
and the browser merges it over the model it already built, so changing a
mapping on a large layer costs nothing beyond the config itself.
}
\examples{
df <- data.frame(species = c("a", "b", "c"), mass = c(1, 5, 3))
chart <- arc_col(df, species, mass)

# Inside a Shiny server:
if (interactive()) {
  arc_proxy("chart", chart) |>
    set_color(species) |>
    arc_update()
}
}
