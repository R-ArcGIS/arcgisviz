% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/arc-proxy.R
\name{arc_refresh}
\alias{arc_refresh}
\alias{arc_reset_zoom}
\alias{arc_clear_selection}
\alias{arc_export_image}
\alias{arc_export_csv}
\alias{arc_notify}
\title{Invoke a rendered chart's own methods}
\usage{
arc_refresh(proxy, update_data = TRUE, reset_axes = FALSE)

arc_reset_zoom(proxy)

arc_clear_selection(proxy)

arc_export_image(proxy, format = "png")

arc_export_csv(proxy)

arc_notify(proxy, message, heading = NULL)
}
\arguments{
\item{proxy}{Defines which \code{\link[=arc_proxy]{arc_proxy()}} to act on.}

\item{update_data}{default \code{TRUE}. Defines whether \code{arc_refresh()}
requeries the layer or only redraws.}

\item{reset_axes}{default \code{FALSE}. Defines whether axis bounds are
recomputed.}

\item{format}{default \code{"png"}. Defines the image format, one of \code{"png"},
\code{"jpeg"}, or \code{"svg"}.}

\item{message, heading}{Defines the text \code{arc_notify()} shows in the
chart's own info panel.}
}
\value{
\code{proxy}, invisibly.
}
\description{
Each of these calls a method on the \verb{<arcgis-chart>} element directly.
The two export functions download in the reader's browser.
}
\examples{
df <- data.frame(species = c("a", "b", "c"), mass = c(1, 5, 3))

if (interactive()) {
  arc_proxy("chart", arc_col(df, species, mass)) |>
    arc_reset_zoom()
}
}
